diff --git a/src/backend/render/mod.rs b/src/backend/render/mod.rs
index 0e22c8ea..2395f8d5 100644
--- a/src/backend/render/mod.rs
+++ b/src/backend/render/mod.rs
@@ -853,7 +853,7 @@ where
                     .map(Into::into),
                 );
             }
-            Stage::LayerSurface { layer, location } => {
+            Stage::LayerSurface { layer, location, alpha } => {
                 elements.extend(
                     render_elements_from_surface_tree::<_, WorkspaceRenderElement<_>>(
                         renderer,
@@ -863,7 +863,7 @@ where
                             .as_logical()
                             .to_physical_precise_round(scale),
                         Scale::from(scale),
-                        1.0,
+                        alpha,
                         FRAME_TIME_FILTER,
                     )
                     .into_iter()
diff --git a/src/input/mod.rs b/src/input/mod.rs
index c57eeca4..7fc681a3 100644
--- a/src/input/mod.rs
+++ b/src/input/mod.rs
@@ -2091,7 +2091,7 @@ impl State {
                             }
                         }
                     }
-                    Stage::LayerSurface { layer, location } => {
+                    Stage::LayerSurface { layer, location, .. } => {
                         if under_from_surface_tree(
                             layer.wl_surface(),
                             global_pos.as_logical(),
@@ -2247,7 +2247,7 @@ impl State {
                             ))));
                         }
                     }
-                    Stage::LayerSurface { layer, location } => {
+                    Stage::LayerSurface { layer, location, .. } => {
                         let surface = layer.wl_surface();
                         if let Some((surface, surface_loc)) = under_from_surface_tree(
                             surface,
diff --git a/src/shell/element/stack/tabs.rs b/src/shell/element/stack/tabs.rs
index fe084b55..4d3d07ad 100644
--- a/src/shell/element/stack/tabs.rs
+++ b/src/shell/element/stack/tabs.rs
@@ -22,7 +22,7 @@ use cosmic::{
 };
 use keyframe::{
     ease,
-    functions::{EaseInOutCubic, EaseOutCubic},
+    functions::EaseOutCubic,
 };
 use std::{
     collections::{HashMap, HashSet, VecDeque},
@@ -248,7 +248,7 @@ impl State {
                     .as_millis() as f32
                     / SCROLL_ANIMATION_DURATION.as_millis() as f32;
 
-                ease(EaseInOutCubic, 0.0, 1.0, percentage)
+                ease(EaseOutCubic, 0.0, 1.0, percentage)
             };
 
             Vector::new(
diff --git a/src/shell/focus/order.rs b/src/shell/focus/order.rs
index 81f23c62..3ec3b6c8 100644
--- a/src/shell/focus/order.rs
+++ b/src/shell/focus/order.rs
@@ -1,7 +1,7 @@
 use std::{ops::ControlFlow, time::Instant};
 
 use cosmic_comp_config::workspace::WorkspaceLayout;
-use keyframe::{ease, functions::EaseInOutCubic};
+use keyframe::{ease, functions::EaseOutCubic};
 use smithay::{
     desktop::{LayerSurface, PopupKind, PopupManager, layer_map_for_output},
     output::{Output, OutputNoMode},
@@ -36,6 +36,7 @@ pub enum Stage<'a> {
     LayerSurface {
         layer: LayerSurface,
         location: Point<i32, Global>,
+        alpha: f32,
     },
     OverrideRedirect {
         surface: &'a X11Surface,
@@ -98,7 +99,7 @@ fn render_input_order_internal<R: 'static>(
         })?;
     }
     for (layer, location) in layer_surfaces(output, Layer::Overlay, element_filter) {
-        callback(Stage::LayerSurface { layer, location })?;
+        { let alpha = shell.layer_alpha(layer.wl_surface()); callback(Stage::LayerSurface { layer, location, alpha })?; }
     }
 
     // calculate a bunch of stuff for workspace transitions
@@ -150,7 +151,7 @@ fn render_input_order_internal<R: 'static>(
                 WorkspaceDelta::Shortcut(st) => (
                     *previous_idx < current.1,
                     ease(
-                        EaseInOutCubic,
+                        EaseOutCubic,
                         0.0,
                         1.0,
                         Instant::now().duration_since(*st).as_millis() as f32
@@ -312,7 +313,7 @@ fn render_input_order_internal<R: 'static>(
     if !has_focused_fullscreen {
         // top-layer shell
         for (layer, location) in layer_surfaces(output, Layer::Top, element_filter) {
-            callback(Stage::LayerSurface { layer, location })?;
+            { let alpha = shell.layer_alpha(layer.wl_surface()); callback(Stage::LayerSurface { layer, location, alpha })?; }
         }
 
         // sticky windows
@@ -344,7 +345,7 @@ fn render_input_order_internal<R: 'static>(
         // bottom layer
         for (layer, mut location) in layer_surfaces(output, Layer::Bottom, element_filter) {
             location += current_offset.as_global();
-            callback(Stage::LayerSurface { layer, location })?;
+            { let alpha = shell.layer_alpha(layer.wl_surface()); callback(Stage::LayerSurface { layer, location, alpha })?; }
         }
     }
 
@@ -353,7 +354,7 @@ fn render_input_order_internal<R: 'static>(
             // previous bottom layer
             for (layer, mut location) in layer_surfaces(output, Layer::Bottom, element_filter) {
                 location += offset.as_global();
-                callback(Stage::LayerSurface { layer, location })?;
+                { let alpha = shell.layer_alpha(layer.wl_surface()); callback(Stage::LayerSurface { layer, location, alpha })?; }
             }
         }
     }
@@ -362,7 +363,7 @@ fn render_input_order_internal<R: 'static>(
         // background layer
         for (layer, mut location) in layer_surfaces(output, Layer::Background, element_filter) {
             location += current_offset.as_global();
-            callback(Stage::LayerSurface { layer, location })?;
+            { let alpha = shell.layer_alpha(layer.wl_surface()); callback(Stage::LayerSurface { layer, location, alpha })?; }
         }
     }
 
@@ -371,7 +372,7 @@ fn render_input_order_internal<R: 'static>(
             // previous background layer
             for (layer, mut location) in layer_surfaces(output, Layer::Background, element_filter) {
                 location += offset.as_global();
-                callback(Stage::LayerSurface { layer, location })?;
+                { let alpha = shell.layer_alpha(layer.wl_surface()); callback(Stage::LayerSurface { layer, location, alpha })?; }
             }
         }
     }
diff --git a/src/shell/layout/floating/mod.rs b/src/shell/layout/floating/mod.rs
index 582ec1b4..fcb112ed 100644
--- a/src/shell/layout/floating/mod.rs
+++ b/src/shell/layout/floating/mod.rs
@@ -8,7 +8,7 @@ use std::{
 
 use cosmic_comp_config::AppearanceConfig;
 use cosmic_settings_config::shortcuts::action::ResizeDirection;
-use keyframe::{ease, functions::EaseInOutCubic};
+use keyframe::{ease, functions::EaseOutCubic};
 use smithay::{
     backend::renderer::{
         ImportAll, ImportMem, Renderer,
@@ -48,8 +48,8 @@ use crate::{
 mod grabs;
 pub use self::grabs::*;
 
-pub const ANIMATION_DURATION: Duration = Duration::from_millis(200);
-pub const MINIMIZE_ANIMATION_DURATION: Duration = Duration::from_millis(320);
+pub const ANIMATION_DURATION: Duration = Duration::from_millis(250);
+pub const MINIMIZE_ANIMATION_DURATION: Duration = Duration::from_millis(400);
 
 #[derive(Debug, Default)]
 pub struct FloatingLayout {
@@ -68,6 +68,7 @@ enum Animation {
     Tiled {
         start: Instant,
         previous_geometry: Rectangle<i32, Local>,
+        is_open: bool,
     },
     Minimize {
         start: Instant,
@@ -92,7 +93,18 @@ impl Animation {
 
     fn alpha(&self) -> f32 {
         match self {
-            Animation::Tiled { .. } => 1.0,
+            Animation::Tiled { start, is_open, .. } => {
+                if *is_open {
+                    let percentage = Instant::now()
+                        .duration_since(*start)
+                        .min(ANIMATION_DURATION)
+                        .as_secs_f32()
+                        / ANIMATION_DURATION.as_secs_f32();
+                    ease(EaseOutCubic, 0.0, 1.0, percentage)
+                } else {
+                    1.0
+                }
+            }
             Animation::Minimize { start, .. } => {
                 let percentage = Instant::now()
                     .duration_since(*start)
@@ -158,7 +170,7 @@ impl Animation {
             now.duration_since(start).min(duration).as_secs_f64() / duration.as_secs_f64();
 
         ease(
-            EaseInOutCubic,
+            EaseOutCubic,
             EaseRectangle(previous_rect),
             EaseRectangle(target_rect),
             progress,
@@ -382,6 +394,7 @@ impl FloatingLayout {
                     Animation::Tiled {
                         start: Instant::now(),
                         previous_geometry,
+                        is_open: false,
                     },
                 );
             }
@@ -608,6 +621,24 @@ impl FloatingLayout {
                 Animation::Tiled {
                     start: Instant::now(),
                     previous_geometry,
+                    is_open: false,
+                },
+            );
+        } else {
+            // New window entering floating — scale from 85% centered + fade in
+            let target = Rectangle::new(position, win_geo.size);
+            let sw = (target.size.w as f64 * 0.85).round() as i32;
+            let sh = (target.size.h as f64 * 0.85).round() as i32;
+            let prev_geo = Rectangle::new(
+                (target.loc.x + (target.size.w - sw) / 2, target.loc.y + (target.size.h - sh) / 2).into(),
+                (sw, sh).into(),
+            );
+            self.animations.insert(
+                mapped.clone(),
+                Animation::Tiled {
+                    start: Instant::now(),
+                    previous_geometry: prev_geo,
+                    is_open: true,
                 },
             );
         }
@@ -722,7 +753,9 @@ impl FloatingLayout {
             }
             (mapped, geo.loc)
         } else {
-            self.map_internal(window.clone(), Some(position), None, None);
+            let win_geo = window.geometry().as_local();
+            let prev = Rectangle::new(position, win_geo.size);
+            self.map_internal(window.clone(), Some(position), None, Some(prev));
             (window, position)
         }
     }
diff --git a/src/shell/layout/tiling/mod.rs b/src/shell/layout/tiling/mod.rs
index 85efb2ce..3fb06d17 100644
--- a/src/shell/layout/tiling/mod.rs
+++ b/src/shell/layout/tiling/mod.rs
@@ -43,7 +43,7 @@ use cosmic_settings_config::shortcuts::action::{FocusDirection, ResizeDirection}
 use id_tree::{InsertBehavior, MoveBehavior, Node, NodeId, NodeIdError, RemoveBehavior, Tree};
 use keyframe::{
     ease,
-    functions::{EaseInOutCubic, Linear},
+    functions::{EaseOutCubic, Linear},
 };
 use smithay::{
     backend::renderer::{
@@ -77,8 +77,8 @@ mod grabs;
 pub use self::blocker::*;
 pub use self::grabs::*;
 
-pub const ANIMATION_DURATION: Duration = Duration::from_millis(200);
-pub const MINIMIZE_ANIMATION_DURATION: Duration = Duration::from_millis(320);
+pub const ANIMATION_DURATION: Duration = Duration::from_millis(250);
+pub const MINIMIZE_ANIMATION_DURATION: Duration = Duration::from_millis(400);
 pub const MOUSE_ANIMATION_DELAY: Duration = Duration::from_millis(150);
 pub const INITIAL_MOUSE_ANIMATION_DELAY: Duration = Duration::from_millis(500);
 
@@ -4043,7 +4043,7 @@ impl TilingLayout {
                 let percentage = since / total;
                 debug_assert!(!percentage.is_nan());
                 debug_assert!(!percentage.is_infinite());
-                ease(EaseInOutCubic, 0.0, 1.0, percentage)
+                ease(EaseOutCubic, 0.0, 1.0, percentage)
             }
         } else {
             1.0
@@ -4196,7 +4196,7 @@ impl TilingLayout {
         let percentage = if let Some(animation_start) = self.queue.animation_start {
             let percentage = Instant::now().duration_since(animation_start).as_millis() as f32
                 / duration.as_millis() as f32;
-            ease(EaseInOutCubic, 0.0, 1.0, percentage)
+            ease(EaseOutCubic, 0.0, 1.0, percentage)
         } else {
             1.0
         };
@@ -5156,7 +5156,7 @@ fn render_old_tree(
                 if let Some(minimize_geo) = minimize_geo {
                     scaled_geo = Some(
                         ease(
-                            EaseInOutCubic,
+                            EaseOutCubic,
                             EaseRectangle(*original_geo),
                             EaseRectangle(*minimize_geo),
                             percentage,
@@ -5837,7 +5837,7 @@ fn render_new_tree(
                 (
                     if was_minimized {
                         ease(
-                            EaseInOutCubic,
+                            EaseOutCubic,
                             EaseRectangle(old_geo),
                             EaseRectangle(new_geo),
                             percentage,
@@ -5856,7 +5856,21 @@ fn render_new_tree(
                     old_geo != new_geo,
                 )
             } else {
-                (new_geo, percentage, false)
+                let is_new_window = if let Data::Mapped { mapped, .. } = data {
+                    mapped.last_geometry.lock().unwrap().is_none()
+                } else {
+                    false
+                };
+                if is_new_window {
+                    let scale = ease(EaseOutCubic, 0.9_f32, 1.0, percentage);
+                    let w = (new_geo.size.w as f32 * scale).round() as i32;
+                    let h = (new_geo.size.h as f32 * scale).round() as i32;
+                    let x = new_geo.loc.x + (new_geo.size.w - w) / 2;
+                    let y = new_geo.loc.y + (new_geo.size.h - h) / 2;
+                    (Rectangle::new((x, y).into(), (w, h).into()), percentage, true)
+                } else {
+                    (new_geo, percentage, false)
+                }
             };
 
             if let Data::Mapped { mapped, .. } = data {
diff --git a/src/shell/mod.rs b/src/shell/mod.rs
index 6ba300d1..244406c1 100644
--- a/src/shell/mod.rs
+++ b/src/shell/mod.rs
@@ -26,7 +26,7 @@ use cosmic_config::ConfigSet;
 use cosmic_protocols::workspace::v2::server::zcosmic_workspace_handle_v2::TilingState;
 use cosmic_settings_config::shortcuts::action::{Direction, FocusDirection, ResizeDirection};
 use cosmic_settings_config::{shortcuts, window_rules::ApplicationException};
-use keyframe::{ease, functions::EaseInOutCubic};
+use keyframe::{ease, functions::{EaseInCubic, EaseOutCubic}};
 use smithay::{
     backend::{input::TouchSlot, renderer::element::RenderElementStates},
     desktop::{
@@ -111,7 +111,7 @@ use self::{
     },
 };
 
-const ANIMATION_DURATION: Duration = Duration::from_millis(200);
+const ANIMATION_DURATION: Duration = Duration::from_millis(250);
 const GESTURE_MAX_LENGTH: f64 = 150.0;
 const GESTURE_POSITION_THRESHOLD: f64 = 0.5;
 const GESTURE_VELOCITY_THRESHOLD: f64 = 0.02;
@@ -140,14 +140,14 @@ impl OverviewMode {
             OverviewMode::Started(_, start) => {
                 let percentage = Instant::now().duration_since(*start).as_millis() as f32
                     / ANIMATION_DURATION.as_millis() as f32;
-                Some(ease(EaseInOutCubic, 0.0, 1.0, percentage))
+                Some(ease(EaseOutCubic, 0.0, 1.0, percentage))
             }
             OverviewMode::Active(_) => Some(1.0),
             OverviewMode::Ended(_, end) => {
                 let percentage = Instant::now().duration_since(*end).as_millis() as f32
                     / ANIMATION_DURATION.as_millis() as f32;
                 if percentage < 1.0 {
-                    Some(ease(EaseInOutCubic, 1.0, 0.0, percentage))
+                    Some(ease(EaseInCubic, 1.0, 0.0, percentage))
                 } else {
                     None
                 }
@@ -193,14 +193,14 @@ impl ResizeMode {
             ResizeMode::Started(_, start, _) => {
                 let percentage = Instant::now().duration_since(*start).as_millis() as f32
                     / ANIMATION_DURATION.as_millis() as f32;
-                Some(ease(EaseInOutCubic, 0.0, 1.0, percentage))
+                Some(ease(EaseOutCubic, 0.0, 1.0, percentage))
             }
             ResizeMode::Active(_, _) => Some(1.0),
             ResizeMode::Ended(end, _) => {
                 let percentage = Instant::now().duration_since(*end).as_millis() as f32
                     / ANIMATION_DURATION.as_millis() as f32;
                 if percentage < 1.0 {
-                    Some(ease(EaseInOutCubic, 1.0, 0.0, percentage))
+                    Some(ease(EaseInCubic, 1.0, 0.0, percentage))
                 } else {
                     None
                 }
@@ -258,6 +258,8 @@ pub struct PendingLayer {
     pub output: Output,
 }
 
+const LAYER_ANIMATION_DURATION: Duration = Duration::from_millis(200);
+
 #[derive(Debug)]
 pub struct Shell {
     pub workspaces: Workspaces,
@@ -289,6 +291,7 @@ pub struct Shell {
     zoom_state: Option<ZoomState>,
     appearance_conf: AppearanceConfig,
     tiling_exceptions: TilingExceptions,
+    pub layer_map_timestamps: HashMap<WlSurface, Instant>,
 
     #[cfg(feature = "debug")]
     pub debug_active: bool,
@@ -325,7 +328,7 @@ impl WorkspaceDelta {
     }
 
     pub fn new_gesture_end(delta: f64, velocity: f64, forward: bool) -> Self {
-        let params: SpringParams = SpringParams::new(1.0, 1000.0, 0.0001);
+        let params: SpringParams = SpringParams::new(0.9, 400.0, 0.001);
         WorkspaceDelta::GestureEnd {
             start: Instant::now(),
             forward,
@@ -342,6 +345,33 @@ impl WorkspaceDelta {
         WorkspaceDelta::Shortcut(Instant::now())
     }
 
+    pub fn new_shortcut_from_progress(initial_progress: f64) -> Self {
+        let offset = Duration::from_secs_f64(
+            initial_progress * ANIMATION_DURATION.as_secs_f64()
+        );
+        WorkspaceDelta::Shortcut(Instant::now() - offset)
+    }
+
+    pub fn current_progress(&self) -> f64 {
+        match self {
+            WorkspaceDelta::Shortcut(start) => {
+                let elapsed = Instant::now().duration_since(*start).as_secs_f64();
+                let total = ANIMATION_DURATION.as_secs_f64();
+                (elapsed / total).min(1.0)
+            }
+            WorkspaceDelta::GestureEnd { start, spring, .. } => {
+                let t = Instant::now().duration_since(*start);
+                let duration = spring.duration();
+                if duration.is_zero() {
+                    1.0
+                } else {
+                    (t.as_secs_f64() / duration.as_secs_f64()).min(1.0)
+                }
+            }
+            WorkspaceDelta::Gesture { percentage, .. } => *percentage,
+        }
+    }
+
     pub fn is_animating(&self) -> bool {
         matches!(
             self,
@@ -513,12 +543,28 @@ impl WorkspaceSet {
 
         if self.active != idx {
             let old_active = self.active;
+
+            let effective_delta = if animate {
+                if let Some((_, ref prev_delta)) = self.previously_active {
+                    let progress = prev_delta.current_progress();
+                    if progress < 1.0 {
+                        WorkspaceDelta::new_shortcut_from_progress(progress)
+                    } else {
+                        workspace_delta
+                    }
+                } else {
+                    workspace_delta
+                }
+            } else {
+                workspace_delta
+            };
+
             state.remove_workspace_state(&self.workspaces[old_active].handle, WState::Active);
             state.remove_workspace_state(&self.workspaces[old_active].handle, WState::Urgent);
             state.remove_workspace_state(&self.workspaces[idx].handle, WState::Urgent);
             state.add_workspace_state(&self.workspaces[idx].handle, WState::Active);
             self.previously_active = if animate {
-                Some((old_active, workspace_delta))
+                Some((old_active, effective_delta))
             } else {
                 None
             };
@@ -1598,6 +1644,7 @@ impl Shell {
             appearance_conf: config.cosmic_conf.appearance_settings.clone(),
             zoom_state: None,
             tiling_exceptions,
+            layer_map_timestamps: HashMap::new(),
 
             #[cfg(feature = "debug")]
             debug_active: false,
@@ -2169,6 +2216,21 @@ impl Shell {
                         .is_some_and(|state| state.lock().unwrap().is_animating())
                 })
             })
+            || self.layer_map_timestamps.values().any(|start| Instant::now().duration_since(*start) < LAYER_ANIMATION_DURATION)
+    }
+
+    pub fn layer_alpha(&self, surface: &WlSurface) -> f32 {
+        if let Some(start) = self.layer_map_timestamps.get(surface) {
+            let elapsed = Instant::now().duration_since(*start);
+            if elapsed < LAYER_ANIMATION_DURATION {
+                let progress = elapsed.as_millis() as f32 / LAYER_ANIMATION_DURATION.as_millis() as f32;
+                ease(EaseOutCubic, 0.0, 1.0, progress)
+            } else {
+                1.0
+            }
+        } else {
+            1.0
+        }
     }
 
     pub fn update_animations(&mut self) -> HashMap<ClientId, Client> {
@@ -2179,6 +2241,7 @@ impl Shell {
         for workspace in self.workspaces.spaces_mut() {
             clients.extend(workspace.update_animations());
         }
+        self.layer_map_timestamps.retain(|_, start| Instant::now().duration_since(*start) < LAYER_ANIMATION_DURATION);
         clients
     }
 
@@ -2875,6 +2938,7 @@ impl Shell {
             let mut map = layer_map_for_output(&pending.output);
             map.map_layer(&pending.surface).unwrap();
         }
+        self.layer_map_timestamps.insert(pending.surface.wl_surface().clone(), Instant::now());
         for workspace in self.workspaces.spaces_mut() {
             workspace.tiling_layer.recalculate();
         }
diff --git a/src/shell/workspace.rs b/src/shell/workspace.rs
index b03ac776..81502fd5 100644
--- a/src/shell/workspace.rs
+++ b/src/shell/workspace.rs
@@ -30,7 +30,7 @@ use cosmic::theme::CosmicTheme;
 use cosmic_protocols::workspace::v2::server::zcosmic_workspace_handle_v2::TilingState;
 use id_tree::Tree;
 use indexmap::IndexSet;
-use keyframe::{ease, functions::EaseInOutCubic};
+use keyframe::{ease, functions::EaseOutCubic};
 use smithay::output::WeakOutput;
 use smithay::{
     backend::renderer::{
@@ -72,7 +72,7 @@ use super::{
     layout::tiling::{Data, NodeDesc},
 };
 
-const FULLSCREEN_ANIMATION_DURATION: Duration = Duration::from_millis(200);
+const FULLSCREEN_ANIMATION_DURATION: Duration = Duration::from_millis(300);
 
 // For stable workspace id, generate random 24-bit integer, as a hex string
 // Must be compared with existing workspaces work uniqueness.
@@ -1547,13 +1547,13 @@ impl Workspace {
                         / FULLSCREEN_ANIMATION_DURATION.as_secs_f64();
                     (
                         ease(
-                            EaseInOutCubic,
+                            EaseOutCubic,
                             EaseRectangle(*previous_geo),
                             EaseRectangle(fullscreen_geo),
                             duration,
                         )
                         .0,
-                        ease(EaseInOutCubic, 0.0, 1.0, duration),
+                        ease(EaseOutCubic, 0.0, 1.0, duration),
                     )
                 }
                 (_, Some(ended)) => {
@@ -1561,13 +1561,13 @@ impl Workspace {
                         / FULLSCREEN_ANIMATION_DURATION.as_secs_f64();
                     (
                         ease(
-                            EaseInOutCubic,
+                            EaseOutCubic,
                             EaseRectangle(fullscreen_geo),
                             EaseRectangle(*previous_geo),
                             duration,
                         )
                         .0,
-                        ease(EaseInOutCubic, 1.0, 0.0, duration),
+                        ease(EaseOutCubic, 1.0, 0.0, duration),
                     )
                 }
                 (None, None) => (fullscreen_geo, 1.0),
@@ -1623,15 +1623,15 @@ impl Workspace {
             // floating surfaces
             let alpha = match &overview.0 {
                 OverviewMode::Started(_, started) => {
-                    (1.0 - (Instant::now().duration_since(*started).as_millis()
-                        / ANIMATION_DURATION.as_millis()) as f32)
+                    (1.0 - (Instant::now().duration_since(*started).as_millis() as f32
+                        / ANIMATION_DURATION.as_millis() as f32))
                         .max(0.0)
                         * 0.4
                         + 0.6
                 }
                 OverviewMode::Ended(_, ended) => {
-                    ((Instant::now().duration_since(*ended).as_millis()
-                        / ANIMATION_DURATION.as_millis()) as f32)
+                    ((Instant::now().duration_since(*ended).as_millis() as f32
+                        / ANIMATION_DURATION.as_millis() as f32))
                         * 0.4
                         + 0.6
                 }
@@ -1748,13 +1748,13 @@ impl Workspace {
                         / FULLSCREEN_ANIMATION_DURATION.as_secs_f64();
                     (
                         ease(
-                            EaseInOutCubic,
+                            EaseOutCubic,
                             EaseRectangle(*previous_geo),
                             EaseRectangle(fullscreen_geo),
                             duration,
                         )
                         .0,
-                        ease(EaseInOutCubic, 0.0, 1.0, duration),
+                        ease(EaseOutCubic, 0.0, 1.0, duration),
                     )
                 }
                 (_, Some(ended)) => {
@@ -1762,13 +1762,13 @@ impl Workspace {
                         / FULLSCREEN_ANIMATION_DURATION.as_secs_f64();
                     (
                         ease(
-                            EaseInOutCubic,
+                            EaseOutCubic,
                             EaseRectangle(fullscreen_geo),
                             EaseRectangle(*previous_geo),
                             duration,
                         )
                         .0,
-                        ease(EaseInOutCubic, 1.0, 0.0, duration),
+                        ease(EaseOutCubic, 1.0, 0.0, duration),
                     )
                 }
                 (None, None) => (fullscreen_geo, 1.0),
@@ -1804,15 +1804,15 @@ impl Workspace {
             // floating surfaces
             let alpha = match &overview.0 {
                 OverviewMode::Started(_, started) => {
-                    (1.0 - (Instant::now().duration_since(*started).as_millis()
-                        / ANIMATION_DURATION.as_millis()) as f32)
+                    (1.0 - (Instant::now().duration_since(*started).as_millis() as f32
+                        / ANIMATION_DURATION.as_millis() as f32))
                         .max(0.0)
                         * 0.4
                         + 0.6
                 }
                 OverviewMode::Ended(_, ended) => {
-                    ((Instant::now().duration_since(*ended).as_millis()
-                        / ANIMATION_DURATION.as_millis()) as f32)
+                    ((Instant::now().duration_since(*ended).as_millis() as f32
+                        / ANIMATION_DURATION.as_millis() as f32))
                         * 0.4
                         + 0.6
                 }
diff --git a/src/shell/zoom.rs b/src/shell/zoom.rs
index fc018c48..df2a57f5 100644
--- a/src/shell/zoom.rs
+++ b/src/shell/zoom.rs
@@ -9,7 +9,7 @@ use cosmic::{
 };
 use cosmic_comp_config::ZoomMovement;
 use cosmic_config::ConfigSet;
-use keyframe::{ease, functions::EaseInOutCubic};
+use keyframe::{ease, functions::EaseOutCubic};
 use smithay::{
     backend::renderer::{ImportMem, Renderer, element::AsRenderElements},
     desktop::space::SpaceElement,
@@ -135,7 +135,7 @@ impl OutputZoomState {
             let percentage =
                 duration_since.as_millis() as f32 / ANIMATION_DURATION.as_millis() as f32;
             ease(
-                EaseInOutCubic,
+                EaseOutCubic,
                 EasePoint(*old_point),
                 EasePoint(self.focal_point),
                 percentage,
@@ -159,7 +159,7 @@ impl OutputZoomState {
             let percentage = Instant::now().duration_since(*start).as_millis() as f32
                 / ANIMATION_DURATION.as_millis() as f32;
 
-            ease(EaseInOutCubic, *old_level, self.level, percentage)
+            ease(EaseOutCubic, *old_level, self.level, percentage)
         } else {
             self.level
         }
diff --git a/src/wayland/handlers/layer_shell.rs b/src/wayland/handlers/layer_shell.rs
index 06be1db4..cd0954ca 100644
--- a/src/wayland/handlers/layer_shell.rs
+++ b/src/wayland/handlers/layer_shell.rs
@@ -73,6 +73,7 @@ impl WlrLayerShellHandler for State {
                 map.unmap_layer(&layer);
             }
 
+            shell.layer_map_timestamps.remove(surface.wl_surface());
             shell.workspaces.recalculate();
 
             self.backend.schedule_render(&output);
