diff --git a/src/shell/layout/tiling/mod.rs b/src/shell/layout/tiling/mod.rs
index 85efb2c..4c62550 100644
--- a/src/shell/layout/tiling/mod.rs
+++ b/src/shell/layout/tiling/mod.rs
@@ -5087,11 +5087,18 @@ where
             }));
             let radius = mapped.corner_radius(geo.size.as_logical(), indicator_thickness);
             if is_minimizing && indicator_thickness > 0 {
+                let mut indicator_geo = geo;
+                let outer_gap = theme.gaps.0 as i32;
+                let t = indicator_thickness as i32;
+                if outer_gap < t {
+                    indicator_geo.loc += (t, t).into();
+                    indicator_geo.size -= (t * 2, t * 2).into();
+                }
                 elements.push(CosmicMappedRenderElement::FocusIndicator(
                     IndicatorShader::focus_element(
                         renderer,
                         Key::Window(Usage::FocusIndicator, mapped.clone().key()),
-                        geo,
+                        indicator_geo,
                         indicator_thickness,
                         radius,
                         alpha,
@@ -5475,6 +5482,16 @@ where
                         .unwrap_or(false)
                         || focused.as_ref() == Some(&node_id)
                     {
+                        let eff_thickness = if data.is_group() { 4u8 } else { indicator_thickness };
+                        let mut indicator_geo = geo;
+                        if !data.is_group() {
+                            let outer_gap = theme.gaps.0 as i32;
+                            let t = eff_thickness as i32;
+                            if outer_gap < t {
+                                indicator_geo.loc += (t, t).into();
+                                indicator_geo.size -= (t * 2, t * 2).into();
+                            }
+                        }
                         indicators.push(CosmicMappedRenderElement::FocusIndicator(
                             IndicatorShader::focus_element(
                                 renderer,
@@ -5485,12 +5502,8 @@ where
                                     Data::Group { alive, .. } => Key::Group(Arc::downgrade(alive)),
                                     _ => unreachable!(),
                                 },
-                                geo,
-                                if data.is_group() {
-                                    4
-                                } else {
-                                    indicator_thickness
-                                },
+                                indicator_geo,
+                                eff_thickness,
                                 radius,
                                 alpha,
                                 output_scale,
